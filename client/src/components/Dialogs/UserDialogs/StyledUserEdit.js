import styled from "styled-components";

const Form = styled.form`
  .input-container {
    display: flex;
    justify-self: center;
    align-items: center;
    justify-content: flex-start;
  }
  .input-field {
    width: 300px;
  }
  .icon {
    margin-top: 10px;
    margin-right: 10px;
  }
`;

export default Form;
